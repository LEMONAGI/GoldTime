# GoldTime TODO — 작업 상태 보드

> 세션 복귀용 단일 출처: "뭐 하던 중이었지 / 다음 뭐 하지 / 실기기로 뭘 확인해야 하지".
> 규칙: 작업 시작 시 "하는 중"에 올리고, 끝나면 **지운다**(완료 기록은 git log가 담당 —
> 여기 쌓지 않는다). 실기기 검증이 남은 채 끝난 작업은 "실기기 검증 대기"에 항목을 남긴다.

## 하는 중

_(없음 — 1.2.0 요일별 제한 규칙은 `Feat/WeekdayRules`에 코드 완료, 실기기 검증 대기)_

## 다음 할 일

- [ ] 대시보드(goldtime-dashboard): 1.2.0 신규 분석 값 처리 — rule_kind "weekday",
      weekday_restricted_days(days_N), 코호트 uses_weekday (shield_hit은 집행 규칙 유지라
      rule_kind 조인 시 의미 분화 주의). 광고 placement `group_edit_gate` 노출 의미가
      1.2.0에서 '편집 진입'→'변경 적용(완료 게이트)'으로 변경 — 노출 수 감소는 UX 개선이지
      이탈이 아님(추이 해석 주석 필요)
- [ ] 시간대 차단 규칙 편집 UX: 시간대 작성 편의성 개선(입력 흐름 다듬기 — 범위 미정)
- [ ] EU 출시 외부 작업: DSA 거래자 정보, AdMob GDPR 메시지 게시, 처리방침 호스팅
- [ ] Firebase: Run Script 추가·Crashlytics 활성화·AdMob 연동 링크

## 실기기 검증 대기

- [ ] **1.2.0 요일별 제한 규칙** (2026-07-11 main 머지 — **검증 완료 전 App Store 제출 금지**)
      런북·판정 기록: [docs/verify-1.2.0-runbook.md](docs/verify-1.2.0-runbook.md)
  - [x] ① daily(토)→쿨다운(일) 전환 — 자정 재무장 `예산 측정 등록 … gen=1`(gen bump 정상),
        cdtick 1분 간격 도착·예산 소진 휴식 진입·90% 알림 (2026-07-12 00:00·00:24 로그)
  - [x] ② timeWindows(토, 잠긴 상태)→제한 없음(일) — `요일 전환 오늘 비추적 → window 정리`
        + `Shield 해제(잠긴 그룹 없음)`, 앱 열림 확인
  - [ ] ③ 전 그룹 '제한 없음' 요일: 하트비트 생존 + 다음날 자정 재무장
        → **월(7/13) 전 그룹 제한없음 → 화(7/14) daily 복귀** 배치로 대기 중.
        7/14 아침에 `sudo log collect --device --last 36h`로 판정
  - [x] ④ 요일마다 다른 timeWindows — `요일 전환 window 재등록`(구성 다름) /
        `시간대 구성 동일 → 등록 기록만 복원`(구성 같음) 두 분기 모두 확인
  - [x] ④-b 일요일 07:00~09:00 시간대 진입 잠금 — `시간대 진입 → 잠금 … newlyLocked=1`(07:00)
        + `시간대 종료 → 해제 점검`·`Shield 해제`(09:01), 사람 개입 없이 자동 발화
  - [x] ⑤ 오늘 규칙 즉시 편집 — 한도 0분 즉시 잠금 / '제한 없음' 즉시 해제,
        완료 시점 광고 게이트(진입 무료·변경 완료 시 안내 카드→광고→커밋) 정상
  - [x] ⑥ 시간대 알림 pending 개수 = 28개(64 제한 여유, dedupe 정상)
  - [x] ⑥-b 요일 트리거 알림 발송 — 일요일(weekday=1) 트리거로 5분 전 알림 정상 도착(12:06).
        06:55 알림 미수신은 수면 시간대 iOS Focus의 조용한 전달로 판단(앱 버그 아님)
  - [x] ⑦ 23:45+ 편집 시 자정 안내 표시됨 — **단 false positive 발견**(아래 별도 항목)
  - [x] ⑧ 1.1.2 → 1.2.0 업그레이드: 그룹 2개 보존, 사용량 승계, 요일 토글 OFF, 재등록 정상

- [ ] **[Fix] 자정 안내 false positive — 코드 수정 완료(2026-07-12), 재확인 대기** —
      커밋 3경로(요일/일일/쿨다운)에 "오늘 `resolved(on:)` 투영 변경 시에만 안내" 조건 추가
      (`ContentViewModel.changesTodayProjection`, churn 가드와 같은 술어). 단위 테스트로
      다른 요일만 편집·무변경 완료 = 안내 없음 / 오늘 편집 = 안내 유지 확인됨.
  - [ ] 재확인(시뮬 가능): 23:45+ 오늘 아닌 요일만 편집 → 광고 게이트는 뜨되 자정 안내 없음 /
        오늘 규칙 편집 → 자정 안내 표시
- [ ] **2026-07-12 UX 4종 변경** (요일 그룹 용어·스트립 탭·배지·시트 dismiss — 시뮬 가능)
  - [ ] 오늘 '제한 없음' 요일 그룹 카드: 배지 "23:59까지 사용 가능" + 초록
        checkmark.shield("설정 필요" 오표시 없음)
  - [ ] 규칙 편집 시트: 드래그/배경 탭으로 내려감(= 취소, 편집 내용 버려짐·광고 없음),
        완료 경로의 staged alert(즉시 잠금 경고·자정 안내)는 기존대로 동작
  - [ ] UI 육안(시뮬레이터 가능): 요일 토글 → 요일 그룹 추가 → 칩 다중 선택 → 규칙 지정 흐름,
        즉시 잠금 경고의 시트 닫힘→alert 타이밍, 문구가 "요일 그룹"(ko)·"曜日グループ"(ja)로 표시
  - [ ] UI 육안 — 주간 스트립(시뮬레이터 가능): 토글 ON 시 7칸 스트립 표시(규칙별
        아이콘·오늘 캡슐 강조), 칸 탭 → **그 요일 하루만 선택된** 편집 push(같은 규칙의
        다른 요일 비포함 — 2026-07-12 변경), 저장 후 스트립 즉시 갱신,
        주 시작 요일(일/월) 변경 시 순서 반영, 다크 모드·큰 글자 레이아웃
  - [ ] UI 육안 — implicit/explicit '제한 없음'(시뮬레이터 가능): ⓐ신규 그룹 토글 ON →
        요일 그룹 행 0개·중립 footer(빨강 아님)·완료 disabled / 기존 규칙 그룹 토글 ON →
        현행대로 단일 요일 그룹 시드 ⓑ스트립 대시(implicit) 탭 → 그 요일만 선택 + '제한 없음'
        선택 상태 진입 → 저장 없이 뒤로 = 무변화, 무변경 완료 = "제한 없음" 행 생성 →
        그 행 재탭 시 그 하루만 선택된 편집(2026-07-12 변경) ⓒ완료 커밋 → 시트 재진입 시
        explicit 행 유지(persist 왕복) / 요일 그룹에서 요일 해제 → 행 축소 + 스트립 대시 /
        토글 OFF→ON 시 편집 내용 복원(같은 시트 세션 — 시트 재진입 후 첫 ON은 시드),
        다크 모드·큰 글자
  - [ ] UI 육안 — 완료 시점 광고 게이트(시뮬 가능·실제 광고 표시는 실기기): ⓐ적용 그룹
        규칙 편집 진입/주간 스트립 탭 = 광고·안내 없음(보기 무료) ⓑ무변경 완료·implicit 탭
        무변경 완료(explicit 승격) = 광고 없이 커밋 ⓒ변경 후 완료 → 안내 카드(cover 1단계,
        "광고 보고 변경하기") → 광고 → 보상 후 커밋·시트 닫힘, 즉시 잠금 경고·자정 안내는
        광고 뒤 순서대로 표시(재표시·누락 없음) ⓓ안내 카드 취소·배경 탭/광고 취소 →
        시트·편집 내용 유지 ⓔdraft 그룹은 변경 완료도 광고·안내 없음 ⓕ앱 선택 편집·삭제는
        기존대로 진입 전 다이얼로그+광고, 카드 다크 모드·큰 글자

## 아이디어 / 언젠가

- [ ] pre-commit 아키텍처 게이트(역방향 import·`@Published`·hex literal 검사) —
      커밋 리뷰를 skim으로 줄이고 싶어지거나 규칙 위반이 실제로 한 번 새는 날 도입
